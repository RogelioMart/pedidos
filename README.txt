README

This program is intended to get information and input it into excel
it also uses a GUI in order to enables these functionalities install the
following

if your pip is a little old consider upgrading by using ´python -m pip install --upgrade pip´

pip install requests

pip install openpyxl